using UnityEngine;

public class Nand_Test : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public CableManager input_a;
    public CableManager input_b;
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
